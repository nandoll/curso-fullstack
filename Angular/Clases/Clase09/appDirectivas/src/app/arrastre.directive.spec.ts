import { ArrastreDirective } from './arrastre.directive';

describe('ArrastreDirective', () => {
  it('should create an instance', () => {
    const directive = new ArrastreDirective();
    expect(directive).toBeTruthy();
  });
});
